# PaatraSetu
It's a project for AMIHACKS, But also a foundation for something bigger, Something that can be implemented at a large scale and can be made into an organization. It contains all the Ideas we have gathered as a team and the various ways that it can be solved.
# What it can do?
1) It can create Individual accounts for volunteers, Restraunts and Individuals.
2) Can connect with volunteers nearby, by a particular pickup radius.
3) Can let a volunteer accept a donation and then mark it as picked up.
4) Uses exact GPS coordinates and uses it with pincode, to help in nearby matching.
5) Can upload restraunt or food stall proof of verification for better trust.
# What does it use? 
1) It uses a Python server which provides the shared API and SQLite database to store the credential and other information.
2) Github pages to display the fronted.
# Project structure:
in docs/
  index.html - contails the App Shell
  app.js     - User interface and browser-side behavior
  styles.css - Layout and styling
  server.py  - Python HTTP server and JSON API
  assets/    - Logos and other visual assets

  ________

  This project currently lacks the backend to store data across the internet, a custom domain, and the API to send SMS, Which can all be definetly implemented once the project picks up pace.
